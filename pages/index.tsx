import type { NextPage } from "next";

import { HomePage } from "../container";

const Home: NextPage = (): JSX.Element => {
    return (
        <>
            <HomePage />
        </>
    );
};

export default Home;
